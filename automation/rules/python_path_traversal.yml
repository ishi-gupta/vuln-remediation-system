rules:
  # ============================================================
  # Path Traversal Detection — Taint Analysis (CWE-22)
  # Tracks user input flowing into file system operations
  # ============================================================

  - id: python-path-traversal-taint
    mode: taint
    message: >
      User-controlled input flows into a file system operation without validation.
      Validate and sanitize file paths: use os.path.realpath() to resolve symlinks,
      then verify the resolved path starts with the expected base directory.
    languages: [python]
    severity: ERROR
    metadata:
      cwe:
        - "CWE-22: Path Traversal"
      confidence: HIGH
      impact: HIGH
      category: security
      technology:
        - python
    pattern-sources:
      - patterns:
          - pattern: |
              def $FUNC(..., $PARAM, ...):
                  ...
          - focus-metavariable: $PARAM
    pattern-sinks:
      # open()
      - patterns:
          - pattern: open($PATH, ...)
          - focus-metavariable: $PATH
      # os.path.join (still vulnerable if user controls components)
      - patterns:
          - pattern: os.path.join(..., $PATH, ...)
          - focus-metavariable: $PATH
      # os.makedirs
      - patterns:
          - pattern: os.makedirs($PATH, ...)
          - focus-metavariable: $PATH
      # os.remove / os.unlink
      - patterns:
          - pattern: os.remove($PATH)
          - focus-metavariable: $PATH
      - patterns:
          - pattern: os.unlink($PATH)
          - focus-metavariable: $PATH
      # shutil operations
      - patterns:
          - pattern: shutil.copy($SRC, ...)
          - focus-metavariable: $SRC
      - patterns:
          - pattern: shutil.move($SRC, ...)
          - focus-metavariable: $SRC
      # pathlib
      - patterns:
          - pattern: Path($PATH)
          - focus-metavariable: $PATH
      # shelve.open — uses pickle internally, path controls which shelf file
      # Moved here from python_deserialization.yml to avoid cross-sanitizer leakage
      - patterns:
          - pattern: shelve.open($PATH)
          - focus-metavariable: $PATH
    pattern-sanitizers:
      # os.path.basename() strips directory components, making traversal impossible
      - pattern: os.path.basename(...)
      # Note: os.path.realpath() alone is NOT a sanitizer — it resolves symlinks
      # but the result can still point anywhere. The real mitigation is
      # realpath + startswith check, which Semgrep taint can't express as a
      # multi-step sanitizer. We intentionally do NOT list realpath here.
      - pattern: int(...)
